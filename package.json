{
  "name": "canticle-web-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dev-only test harness for the served web/static/js assets. Nothing here is bundled, shipped, or embedded: the browser loads web/static/js/*.js verbatim and web/static/embed.go embeds those same files, so these stay devDependencies and the repo keeps its no-build-step-for-JS property.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^27.0.0",
    "vitest": "^3.2.4"
  }
}
