# Version is derived from git tags via GoReleaser ldflags (stamped into
# internal/version at build time); there is no checked-in version file.

[build]
working_dir = "."
pre_checks = [
  "go test -count=1 ./...",
  "golangci-lint run",
]

[release]
tag_prefix = "v"
